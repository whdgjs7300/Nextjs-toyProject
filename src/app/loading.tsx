import LoadingSpinner from "@/components/LodingSpinner";


export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return <LoadingSpinner/> // 로딩 중에 표시할 UI를 반환
}