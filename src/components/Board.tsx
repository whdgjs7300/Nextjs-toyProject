'use client'
import { useState, useEffect } from 'react';
import styles from '../CSS/Board.module.css';
import Modal from './Modal';

type DataProps = {
    code : string | undefined | null,
} 

export default function Board({code} : DataProps) {
    
    console.log(code)
    
return (
    <div>

    </div>
)
}
